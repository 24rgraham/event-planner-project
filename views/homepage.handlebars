<h1>Homepage</h1>
  <section class="flex-row justify-center">
    <!-- For each event, the event-details partial is used to render each event's data -->
    {{#each events}}
    {{> event-details}}
    {{/each}}
  </section>
</section>